// এটা একটা placeholder test। main.dart এখন Hive ও Provider এর উপর নির্ভরশীল
// বলে পুরো app pump করে টেস্ট করতে হলে test-environment এ Hive সেটআপ লাগবে —
// সেটা পরে (testing sprint-এ) ঠিকভাবে যোগ করা হবে।
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder test', () {
    expect(1 + 1, 2);
  });
}
